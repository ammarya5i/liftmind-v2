# Supabase (safe to expose to the browser — anon key is RLS-protected)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only secrets — never prefix these with NEXT_PUBLIC.
# Used from Phase 2 (coach) and Phase 3 (memory embeddings) onward.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
